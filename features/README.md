# features/

Reserved for feature-scoped modules as the app grows — each feature bundles its
own components, hooks and logic (e.g. `features/sites/`, `features/photos/`,
`features/reports/`).

Currently shared components live in `components/`. Move a feature here once it
grows large enough to own multiple files.
