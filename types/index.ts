/**
 * Central type barrel. Domain types currently live in `lib/types.ts`;
 * import from `@/types` going forward so the source can migrate here later
 * without touching call sites.
 */
export * from "@/lib/types";
